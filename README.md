# How Bitter is the Beer?

**Kaggle Competition:** [Beer 2019](https://www.kaggle.com/competitions/beer2019/overview)

---

## 📋 Описание Задачи (Problem Statement)

Мы решаем задачу **регрессии**, в которой необходимо предсказать **IBU (International Bitterness Units)** — показатель горечи пива — на основе других характеристик пива.

**IBU (International Bitterness Units)** — это шкала международных единиц измерения горькости, используемая для количественной оценки горечи пива. Чем выше значение IBU, тем более горьким является пиво.

**Цель:** Построить модель машинного обучения для прогнозирования IBU пива с учетом его характеристик, включая цвет (SRM), содержание алкоголя (ABV), оригинальную плотность (originalGravity) и другие признаки.

---

## 📊 Данные (Data)

### File Descriptions

- **`beer_train.csv`** — обучающая выборка с целевой переменной `ibu`
- **`beer_test.csv`** — тестовая выборка без целевой переменной
- **`beer_test_sample_submission.csv`** — пример файла отправки в правильном формате

### Data Fields

| Поле | Описание | Тип |
|------|----------|-----|
| `id` | Уникальный идентификатор для каждого пива | int/string |
| `name` | Название пива | string |
| `description` | Описание пива | string (text) |
| `available` | Когда пиво доступно | string |
| `isOrganic` | "Y" если пиво органическое, "N" в противном случае | categorical |
| `glass` | Тип бокала, в котором рекомендуется подавать пиво | categorical |
| `abv` | Alcohol By Volume — содержание алкоголя в процентах | float |
| `originalGravity` | Оригинальная плотность пива | float |
| `srm` | SRM (Standard Reference Method) — показатель цвета пива | float |
| `ibu` | **IBU (International Bitterness Units) — целевая переменная** | float |

### Особенности данных

- **Целевая переменная:** `ibu` (присутствует только в обучающей выборке)
- **Текстовые признаки:** `name`, `description` — могут потребовать обработки (TF-IDF, embeddings)
- **Категориальные признаки:** `isOrganic`, `glass`, `available` — требуют кодирования
- **Числовые признаки:** `abv`, `originalGravity`, `srm` — могут содержать пропуски

---

## 📈 Метрика Оценки (Evaluation Metric)

**Метрика:** **RMSE (Root Mean Squared Error)**

```
RMSE = sqrt(mean((y_true - y_pred)^2))
```

Каждому сорту пива в обучающих и тестовых данных присвоен уникальный идентификатор. Необходимо отправить прогнозы для тестовых данных в формате:
- Файл должен содержать колонки: `id`, `ibu`
- Формат файла: CSV

---

## 🏗️ Структура Проекта (Project Structure)

Проект организован с четким разделением между исследовательской частью (EDA в ноутбуках) и готовой к деплою частью (FastAPI сервис).

```
├── data/                          # Неизменяемые исходные данные соревнования
│   ├── raw/                       # Оригинальные файлы с Kaggle
│   │   ├── beer_train.csv
│   │   ├── beer_test.csv
│   │   └── beer_test_sample_submission.csv
│   └── processed/                 # Предобработанные данные (опционально)
│
├── notebooks/                     # Jupyter ноутбуки для EDA и экспериментов
│   ├── 01_eda.ipynb               # Исследовательский анализ данных
│   ├── 02_feature_engineering.ipynb  # Инженерия признаков
│   ├── 03_modeling.ipynb          # Обучение и валидация моделей
│   └── 04_submission.ipynb        # Генерация финальных предсказаний
│
├── src/                           # Исходный код для FastAPI сервиса и скриптов
│   ├── api/                       # FastAPI приложение
│   │   ├── __init__.py
│   │   ├── main.py                # Точка входа FastAPI
│   │   ├── models.py              # Pydantic модели для запросов/ответов
│   │   └── routes.py              # API endpoints
│   ├── data/                      # Скрипты для обработки данных
│   │   ├── __init__.py
│   │   ├── preprocessing.py       # Функции предобработки
│   │   └── feature_engineering.py # Инженерия признаков
│   ├── models/                    # Код для обучения и инференса моделей
│   │   ├── __init__.py
│   │   ├── train.py               # Скрипт обучения модели
│   │   └── predict.py             # Функции для предсказаний
│   └── utils/                     # Вспомогательные утилиты
│       ├── __init__.py
│       └── config.py              # Конфигурация проекта
│
├── artifacts/                     # Обученные модели и артефакты
│   ├── models/                    # Сохраненные модели (pickle, joblib, etc.)
│   ├── scalers/                   # Сохраненные скейлеры
│   └── encoders/                  # Сохраненные энкодеры (LabelEncoder, etc.)
│
├── submissions/                   # Сгенерированные файлы для отправки на Kaggle
│   └── submission_*.csv           # Файлы с предсказаниями
│
├── tests/                         # Unit тесты (опционально)
│   └── test_*.py
│
├── Dockerfile                     # Инструкция по сборке Docker-образа для сервиса
├── .dockerignore                  # Исключения для Docker
├── requirements.txt               # Все зависимости проекта (для EDA и сервиса)
├── .gitignore                     # Игнорируемые файлы Git
└── README.md                      # Этот файл
```

---

## 🚀 Быстрый Старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Подготовка данных

Скачайте данные с Kaggle и поместите их в `data/raw/`:
- `beer_train.csv`
- `beer_test.csv`
- `beer_test_sample_submission.csv`

### 3. Исследовательский анализ

Откройте ноутбуки в `notebooks/` для EDA и экспериментов.

### 4. Обучение модели

```bash
python src/models/train.py
```

### 5. Запуск FastAPI сервиса

```bash
uvicorn src.api.main:app --reload
```

Или через Docker:

```bash
docker build -t beer-ibu-predictor .
docker run -p 8000:8000 beer-ibu-predictor
```

---

## 🔍 Ключевые Особенности Задачи

1. **Регрессионная задача:** Предсказание непрерывной величины (IBU)
2. **Смешанные типы данных:** Числовые, категориальные и текстовые признаки
3. **Потенциальные проблемы:**
   - Пропуски в данных (особенно в `abv`, `originalGravity`, `srm`)
   - Выбросы в числовых признаках
   - Несбалансированность категориальных признаков
   - Текстовые данные (`description`) требуют специальной обработки

---

## 📝 Заметки для Разработки

- **Базовый подход:** Начните с простой модели (Linear Regression, Random Forest) на числовых признаках
- **Улучшения:** Добавьте feature engineering для категориальных и текстовых признаков
- **Продвинутые методы:** Рассмотрите использование градиентного бустинга (XGBoost, LightGBM, CatBoost) или нейронных сетей
- **Валидация:** Используйте кросс-валидацию для оценки качества модели
- **Метрика:** Оптимизируйте RMSE, но также отслеживайте MAE для понимания распределения ошибок

---

## 📚 Полезные Ссылки

- [Kaggle Competition Page](https://www.kaggle.com/competitions/beer2019/overview)
- [IBU Wikipedia](https://en.wikipedia.org/wiki/Beer_measurement#Bitterness)
- [SRM Color Scale](https://en.wikipedia.org/wiki/Standard_Reference_Method)

---

## 📄 Лицензия

Этот проект создан для участия в Kaggle соревновании. Данные принадлежат Kaggle и организаторам соревнования.

